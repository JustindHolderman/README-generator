{
  "name": "readme-generator",
  "version": "1.0.0",
  "description": "This is an application that generate a README.md file for deployed applications.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "colors": "^1.4.0",
    "inquirer": "^8.2.4"
  }
}
